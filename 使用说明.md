**BOSS 两步助手 v0.1.1**

你在 Edge 登录 BOSS，选好职位范围，然后点“开始这页”。程序使用 BOSS 自带招呼语，并发送你已上传到 BOSS 的附件简历。

安装一次即可，不需要 Python、AI 密钥或重新上传简历。

1. Chrome 打开 `chrome://extensions/`；Edge 打开 `edge://extensions/`，然后打开“开发者模式”。
2. 点击“加载解压缩的扩展”，选择本项目的 `extension` 文件夹。选择后其中应该能看到 `manifest.json`。
3. 刷新已经登录的 BOSS 页面，进入“职位”页。右下角会出现“打招呼 → 发简历”面板。
4. 先在 BOSS 自己的界面选好岗位范围，再填写本次处理数量和“岗位间隔（秒）”，默认 10 秒，可填 1～600 秒的整数。只有一份附件时，文件名可以留空；多份时填写完整名称。
5. 点击“开始这页”。助手会新开一个工作标签页，依次处理当前已经加载、带有岗位详情链接的职位。请保留工作标签页，运行时让它保持前台，不要手动切换该标签中的会话。

计时从点击“立即沟通”开始。等待期间，“发简历”入口可用且核对到目标会话，就选择并发送 BOSS 中已有的附件。时间到后，如果还不能发附件、会话未打开或无法核对目标会话，就记录原因并自动跳过，打开下一岗位。面板显示剩余等待秒数。页面每秒检查一次，切换时还需等待网页加载，所以实际间隔可能稍长。

如果提前发完附件，也会等到设定间隔结束再打开下一岗位；超时后不会额外再等旧版固定的 5 秒。跳过的岗位不会自动回访补发；对方之后回复时，可在 BOSS 消息页处理。

“暂停”会停止后续动作并冻结倒计时。运行中想调间隔，先暂停、改秒数，再点“继续”；新设置立即用于当前岗位，暂停期间不计入等待时间。设置会保存在本机。“停止”结束本批。已发出的消息或附件不会撤回。

网页验证、附件选择不明确、已经点击发送但结果无法确认时仍会暂停并说明原因，你可以核对后继续或手动跳过。一旦开始发送附件，会先核验结果，不会在等待时间到时再次发送或把未知结果算作成功。发送附件受当前平台会话条件限制，打招呼不等于附件已发送。

从旧版更新：先在旧助手面板点“停止”。如果之前加载的就是本项目 `extension` 文件夹，直接在扩展管理页点击本插件的重新加载按钮；如果加载的是其他位置，将新版 `extension` 中的文件覆盖到原来加载的文件夹后，再点重新加载。然后刷新所有 BOSS 标签页，回职位页开始新一批。无需卸载插件，原插件的处理历史可以保留。

已经沟通过的岗位和本插件已有处理记录的岗位会跳过，避免重复打招呼。关闭浏览器后任务不会自行恢复发送。插件仅作用于 `https://www.zhipin.com/`，没有服务器，也不读取密码或导出 Cookie。

本版已通过本地模拟页面的流程测试，包括超时切换下一岗位、倒计时、暂停修改间隔、刷新后保留倒计时和避免重复发送。你当前 Edge 中的实际职位与聊天页面仍需运行验证。若显示“没有未处理的岗位”或找不到页面入口，请保留错误提示和所在页面，以便针对当前页面调整。源码与记录都在本机；调研中已说明平台对第三方自动化的限制。

开发验证：项目根目录运行 `npm install`，再运行 `npm test`。安装和使用插件本身不需要 npm。
