{
  "name": "boss-two-step-helper",
  "version": "0.1.1",
  "private": true,
  "description": "Local tests for a dependency-free Edge/Chrome extension",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
